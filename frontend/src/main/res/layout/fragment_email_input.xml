<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_cream"
    android:fillViewport="true"
    android:overScrollMode="never">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:paddingTop="24dp"
        android:paddingBottom="40dp">

        <!-- Back button -->
        <ImageButton
            android:id="@+id/btn_back_email"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_back"
            android:contentDescription="Back" />

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:fontFamily="sans-serif-black"
            android:text="@string/forgot_password_title"
            android:textColor="@color/text_dark"
            android:textSize="24sp" />

        <!-- Subtitle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/forgot_password_email_subtitle"
            android:textColor="@color/text_muted"
            android:textSize="14sp" />

        <!-- Google hint -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/forgot_password_google_hint"
            android:textColor="@color/text_muted"
            android:textSize="12sp"
            android:fontFamily="sans-serif-light" />

        <!-- Email field -->
        <com.walkmate.core.designsystem.view.WalkMateInputField
            android:id="@+id/field_reset_email"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            app:wm_hint="hello@example.com"
            app:wm_icon="@drawable/ic_mail"
            app:wm_inputType="textEmailAddress"
            app:wm_label="Email Address" />

        <!-- Send OTP button -->
        <com.walkmate.core.designsystem.view.WalkMateButton
            android:id="@+id/btn_send_otp"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:layout_marginTop="28dp"
            app:wm_buttonStyle="filled"
            app:wm_text="@string/forgot_password_send_otp" />

    </LinearLayout>

</androidx.core.widget.NestedScrollView>
